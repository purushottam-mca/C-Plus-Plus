#ifndef CALCULATOR_H // If it is not defined then only define it ?? WHY  
#define CALCULATOR_H

class Calculator{
    public:
        double add(double a, double b);
        double sub(double a, double b);
        double mult(double a, double b);
        double div(double a, double b);
};

#endif // CALCULATOR_H